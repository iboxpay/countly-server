var plugin = {},
    countlyConfig = require('../../../frontend/express/config');

(function(plugin) {
    plugin.init = function() {
        // app.get(countlyConfig.path + '/funnels/*', function(req, res) {

        // });
    };
}(plugin));

module.exports = plugin;